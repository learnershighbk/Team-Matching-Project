# 🏗️ ARCHITECTURE.md — TeamMatch 시스템 아키텍처

**참조:** PRD.md  
**버전:** v1.1  
**최종 업데이트:** 2025-01-06  
**아키텍처 패턴:** Feature-Sliced Design

---

## 1. System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENTS                                  │
├─────────────┬─────────────────┬─────────────────────────────────┤
│   Admin     │   Instructor    │           Student               │
│  /admin     │   /instructor   │    /course/{uuid}               │
└──────┬──────┴────────┬────────┴──────────────┬──────────────────┘
       │               │                        │
       └───────────────┼────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Next.js 14 (App Router)                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Middleware                            │   │
│  │              (JWT 검증, Role 체크)                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │  API Routes  │  │   Pages      │  │    Components        │  │
│  │  /api/*      │  │  App Router  │  │    UI Library        │  │
│  └──────┬───────┘  └──────────────┘  └──────────────────────┘  │
└─────────┼───────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Supabase                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ PostgreSQL  │  │    Auth     │  │      Storage            │  │
│  │  (Tables)   │  │  (미사용)    │  │      (미사용)            │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Tech Stack

| Layer | Technology | Version | Purpose |
|-------|------------|---------|---------|
| **Frontend** | Next.js | 14.x | App Router, SSR/CSR |
| **Styling** | Tailwind CSS | 3.x | Utility-first CSS |
| **UI Components** | shadcn/ui | latest | 재사용 가능 컴포넌트 |
| **Backend** | Next.js API Routes | 14.x | Serverless Functions |
| **Database** | Supabase (PostgreSQL) | latest | 데이터 저장 |
| **Auth** | 자체 JWT | - | jose 라이브러리 |
| **Deployment** | Vercel | - | Edge Network |
| **Language** | TypeScript | 5.x | Type Safety |

---

## 3. Folder Structure (Feature-Sliced Design)

본 프로젝트는 **Feature-Sliced Design** 패턴을 채택하여, 기능별로 관련 코드를 응집시킵니다.

```
src/
├── app/                            # Next.js App Router (라우팅만 담당)
│   ├── (protected)/                # 인증 필요 공통 레이아웃
│   │   ├── layout.tsx
│   │   └── dashboard/
│   │       └── page.tsx
│   ├── admin/                      # Admin 페이지
│   │   ├── page.tsx                # Admin 로그인
│   │   └── dashboard/
│   │       └── page.tsx            # Admin 대시보드
│   ├── instructor/                 # Instructor 페이지
│   │   ├── page.tsx                # Instructor 로그인
│   │   └── dashboard/
│   │       └── page.tsx            # Instructor 대시보드
│   ├── course/                     # Student 페이지
│   │   └── [uuid]/
│   │       ├── page.tsx            # 학생 인증
│   │       ├── profile/
│   │       │   └── page.tsx        # 프로필 입력/수정
│   │       └── team/
│   │           └── page.tsx        # 팀 결과 확인
│   ├── api/                        # API Routes (Hono catch-all)
│   │   └── [[...hono]]/
│   │       └── route.ts            # 모든 API를 Hono로 위임
│   ├── layout.tsx                  # 루트 레이아웃
│   ├── page.tsx                    # 랜딩 페이지
│   ├── providers.tsx               # 전역 Provider
│   └── globals.css
│
├── features/                       # 🎯 기능별 모듈 (핵심)
│   ├── admin/                      # Admin 기능
│   │   ├── backend/                # API 로직
│   │   │   ├── route.ts            # Hono 라우트 등록
│   │   │   ├── schema.ts           # Zod 검증 스키마
│   │   │   └── service.ts          # 비즈니스 로직
│   │   ├── components/             # Admin 전용 컴포넌트
│   │   ├── hooks/                  # Admin 전용 훅
│   │   └── types.ts                # Admin 타입 정의
│   ├── instructor/                 # Instructor 기능
│   │   ├── backend/
│   │   ├── components/
│   │   ├── hooks/
│   │   └── types.ts
│   ├── student/                    # Student 기능
│   │   ├── backend/
│   │   ├── components/
│   │   ├── hooks/
│   │   └── types.ts
│   ├── course/                     # Course 관련 기능
│   │   ├── backend/
│   │   ├── components/
│   │   └── types.ts
│   ├── matching/                   # 매칭 알고리즘
│   │   ├── algorithm.ts            # 메인 매칭 로직
│   │   ├── scoring.ts              # 점수 계산
│   │   ├── optimizer.ts            # Local Swap 최적화
│   │   ├── slots.ts                # 팀 슬롯 생성 (낙오자 방지)
│   │   ├── weights.ts              # 가중치 프로파일
│   │   └── types.ts
│   └── auth/                       # 인증 기능
│       ├── context/
│       │   └── current-user-context.tsx
│       ├── hooks/
│       │   └── useCurrentUser.ts
│       ├── server/
│       │   └── load-current-user.ts
│       └── types.ts
│
├── backend/                        # Hono 백엔드 공통 설정
│   ├── hono/
│   │   ├── app.ts                  # Hono 앱 인스턴스 & 라우트 등록
│   │   └── context.ts              # Hono Context 타입
│   ├── middleware/
│   │   ├── context.ts              # 앱 컨텍스트 미들웨어
│   │   ├── error.ts                # 에러 처리 미들웨어
│   │   └── supabase.ts             # Supabase 클라이언트 주입
│   ├── http/
│   │   └── response.ts             # 표준 응답 헬퍼
│   └── supabase/
│       └── client.ts               # 서버용 Supabase 클라이언트
│
├── components/                     # 공유 UI 컴포넌트
│   └── ui/                         # shadcn/ui 컴포넌트
│       ├── button.tsx
│       ├── input.tsx
│       ├── card.tsx
│       ├── form.tsx
│       ├── select.tsx
│       └── ...
│
├── lib/                            # 공유 유틸리티
│   ├── auth/
│   │   ├── jwt.ts                  # JWT 생성/검증
│   │   ├── hash.ts                 # bcrypt 해싱
│   │   └── cookie.ts               # Cookie 관리
│   ├── supabase/
│   │   ├── types.ts                # DB 타입 정의
│   │   ├── browser-client.ts       # 브라우저용 클라이언트
│   │   └── server-client.ts        # 서버용 클라이언트
│   ├── remote/
│   │   └── api-client.ts           # 프론트엔드 API 클라이언트
│   └── utils.ts                    # 공통 유틸리티
│
├── constants/                      # 상수 정의
│   ├── auth.ts                     # 인증 관련 상수
│   └── env.ts                      # 환경 변수 검증
│
├── hooks/                          # 공유 훅
│   └── use-toast.ts
│
├── middleware.ts                   # Next.js Middleware (루트)
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── package.json
└── .env.local                      # 환경변수
```

### 3.1 Feature 모듈 구조 (상세)

각 feature는 다음 구조를 따릅니다:

```
features/{feature-name}/
├── backend/                  # 서버 사이드 (API 로직)
│   ├── route.ts             # Hono 라우트 정의
│   ├── schema.ts            # Zod 검증 스키마
│   └── service.ts           # 비즈니스 로직 (DB 쿼리 등)
├── components/              # 클라이언트 컴포넌트
│   └── FeatureComponent.tsx
├── hooks/                   # 클라이언트 훅
│   └── useFeature.ts
├── lib/                     # feature 전용 유틸리티
│   └── helpers.ts
└── types.ts                 # 타입 정의
```

### 3.2 API 라우트 구조 (Hono)

```typescript
// src/backend/hono/app.ts
import { Hono } from 'hono';
import { registerAdminRoutes } from '@/features/admin/backend/route';
import { registerInstructorRoutes } from '@/features/instructor/backend/route';
import { registerStudentRoutes } from '@/features/student/backend/route';
import { registerCourseRoutes } from '@/features/course/backend/route';

const app = new Hono();

// 공통 미들웨어
app.use('*', errorBoundary());
app.use('*', withAppContext());
app.use('*', withSupabase());

// 기능별 라우트 등록
registerAdminRoutes(app);       // /api/admin/*
registerInstructorRoutes(app);  // /api/instructor/*
registerStudentRoutes(app);     // /api/student/*
registerCourseRoutes(app);      // /api/course/*

export { app };
```

---

## 4. Component Architecture

### 4.1 컴포넌트 계층 (Feature-Sliced)

```
┌─────────────────────────────────────────────────────────────┐
│                        Pages                                 │
│  (src/app/*/page.tsx - 라우팅, 데이터 페칭)                  │
│  Server Components (기본)                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  Feature Components                          │
│  (src/features/*/components/* - 기능별 컴포넌트)            │
│  'use client' 필요 시 명시                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    UI Components                             │
│  (src/components/ui/* - shadcn/ui 기반)                     │
│  재사용 가능한 원자 컴포넌트                                 │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 컴포넌트 위치 결정 기준

| 위치 | 기준 | 예시 |
|------|------|------|
| `features/*/components/` | 특정 기능에서만 사용 | `InstructorDashboard`, `ProfileForm` |
| `components/ui/` | 범용 UI, shadcn/ui | `Button`, `Input`, `Card` |
| `app/*/page.tsx` | 라우트 진입점 | 페이지 컴포넌트 |

### 4.2 상태 관리

```
┌─────────────────────────────────────────────────────────────┐
│                    State Strategy                            │
├─────────────────────────────────────────────────────────────┤
│  Server State:  React Query (or SWR) - API 데이터 캐싱      │
│  Client State:  React useState/useReducer - UI 상태         │
│  Form State:    React Hook Form - 폼 상태 관리              │
│  Auth State:    Context API + JWT Cookie                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. API Architecture (Hono)

### 5.1 API Route 패턴

본 프로젝트는 **Hono**를 사용하여 API를 구현합니다. Next.js의 catch-all 라우트(`[[...hono]]`)로 모든 API 요청을 Hono로 위임합니다.

```typescript
// 표준 API Response 형식
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: {
    code: string;    // AUTH_001, COURSE_001 등
    message: string;
  };
}

// Hono 라우트 패턴 (features/*/backend/route.ts)
import { Hono } from 'hono';
import { zValidator } from '@hono/zod-validator';
import { loginSchema } from './schema';
import { AdminService } from './service';

export function registerAdminRoutes(app: Hono) {
  const admin = new Hono();

  // POST /api/admin/login
  admin.post('/login', zValidator('json', loginSchema), async (c) => {
    const body = c.req.valid('json');
    
    const result = await AdminService.login(body.email, body.password);
    
    if (!result.success) {
      return c.json({ 
        success: false, 
        error: { code: 'AUTH_003', message: '인증 실패' } 
      }, 401);
    }
    
    // Cookie 설정
    setCookie(c, 'token', result.token, { httpOnly: true, ... });
    
    return c.json({ success: true, data: result.data });
  });

  app.route('/api/admin', admin);
}
```

### 5.2 미들웨어 체인

```
Request
    │
    ▼
┌─────────────────┐
│  Rate Limiting  │  (선택적)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  JWT Validation │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Role Check     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Input Validate │
└────────┬────────┘
         │
         ▼
    API Handler
```

---

## 6. Database Architecture

### 6.1 테이블 관계도

```
┌─────────────────┐
│   instructors   │
│  (교수자 정보)   │
├─────────────────┤
│ instructor_id   │─────┐
│ email           │     │
│ pin_hash        │     │
│ name            │     │
└─────────────────┘     │
                        │ 1:N
                        ▼
                ┌─────────────────┐
                │     courses     │
                │   (코스 정보)    │
                ├─────────────────┤
          ┌─────│ course_id       │─────┐
          │     │ instructor_id   │     │
          │     │ course_name     │     │
          │     │ status          │     │
          │     │ deadline        │     │
          │     └─────────────────┘     │
          │                             │
          │ 1:N                         │ 1:N
          ▼                             ▼
┌─────────────────┐             ┌─────────────────┐
│     teams       │             │    students     │
│   (팀 정보)      │             │   (학생 정보)    │
├─────────────────┤             ├─────────────────┤
│ team_id         │◄────────────│ student_id      │
│ course_id       │             │ course_id       │
│ team_number     │    N:1      │ team_id (FK)    │
│ score_*         │             │ student_number  │
│ top_factors     │             │ profile fields  │
└─────────────────┘             └─────────────────┘
```

### 6.2 인덱스 전략

```sql
-- 빠른 조회를 위한 인덱스
CREATE INDEX idx_courses_instructor ON courses(instructor_id);
CREATE INDEX idx_courses_status ON courses(status);
CREATE INDEX idx_students_course ON students(course_id);
CREATE INDEX idx_students_team ON students(team_id);
CREATE UNIQUE INDEX idx_students_number_course ON students(course_id, student_number);
CREATE INDEX idx_teams_course ON teams(course_id);
```

---

## 7. Security Architecture

### 7.1 인증 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                    JWT Token Flow                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Login Request                                               │
│       │                                                      │
│       ▼                                                      │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  Validate   │───▶│  Generate   │───▶│  Set Cookie │     │
│  │ Credentials │    │    JWT      │    │  (httpOnly) │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│                                                              │
│  Subsequent Requests                                         │
│       │                                                      │
│       ▼                                                      │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  Extract    │───▶│   Verify    │───▶│  Authorize  │     │
│  │   Cookie    │    │    JWT      │    │    Role     │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 JWT Payload 구조

```typescript
// Admin JWT
interface AdminJWT {
  role: 'admin';
  email: string;
  exp: number;  // 4시간
  iat: number;
}

// Instructor JWT
interface InstructorJWT {
  role: 'instructor';
  instructorId: string;
  email: string;
  exp: number;  // 24시간
  iat: number;
}

// Student JWT
interface StudentJWT {
  role: 'student';
  studentId: string;
  courseId: string;
  studentNumber: string;
  exp: number;  // 24시간
  iat: number;
}
```

### 7.3 접근 제어 매트릭스

| Endpoint | Admin | Instructor | Student |
|----------|-------|------------|---------|
| `GET /api/admin/*` | ✅ | ❌ | ❌ |
| `POST /api/admin/*` | ✅ | ❌ | ❌ |
| `GET /api/instructor/courses` | ❌ | ✅ (본인) | ❌ |
| `POST /api/instructor/courses/:id/match` | ❌ | ✅ (본인) | ❌ |
| `GET /api/student/profile` | ❌ | ❌ | ✅ (본인) |
| `GET /api/student/team` | ❌ | ❌ | ✅ (본인) |
| `GET /api/course/:uuid/status` | ❌ | ❌ | ✅ |

---

## 8. Deployment Architecture

### 8.1 Vercel 배포 구조

```
┌─────────────────────────────────────────────────────────────┐
│                       Vercel                                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │   Edge      │    │  Serverless │    │   Static    │     │
│  │  Network    │───▶│  Functions  │    │   Assets    │     │
│  │   (CDN)     │    │  (API)      │    │  (Next.js)  │     │
│  └─────────────┘    └──────┬──────┘    └─────────────┘     │
│                            │                                 │
└────────────────────────────┼─────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      Supabase                                │
│                   (PostgreSQL)                               │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 환경 구성

```
Environments:
├── Development (local)
│   └── .env.local
├── Preview (PR branches)
│   └── Vercel Preview
└── Production (main branch)
    └── Vercel Production
```

---

## 9. Error Handling Strategy

### 9.1 에러 계층

```
┌─────────────────────────────────────────────────────────────┐
│                    Error Hierarchy                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  AppError (Base)                                             │
│      │                                                       │
│      ├── AuthError                                           │
│      │     ├── AUTH_001 (잘못된 학번 형식)                   │
│      │     ├── AUTH_002 (잘못된 PIN 형식)                    │
│      │     └── AUTH_003 (인증 실패)                          │
│      │                                                       │
│      ├── CourseError                                         │
│      │     ├── COURSE_001 (코스 없음)                        │
│      │     └── COURSE_002 (마감됨)                           │
│      │                                                       │
│      └── MatchError                                          │
│            ├── MATCH_001 (학생 부족)                         │
│            └── MATCH_002 (이미 확정됨)                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 클라이언트 에러 처리

```typescript
// 전역 에러 핸들러
function handleApiError(error: ApiError) {
  switch (error.code) {
    case 'AUTH_003':
      redirect('/login');
      break;
    case 'COURSE_002':
      toast.error('프로필 입력 마감기한이 지났습니다.');
      break;
    default:
      toast.error(error.message);
  }
}
```

---

## 10. Performance Considerations

### 10.1 최적화 전략

| 영역 | 전략 |
|------|------|
| **Database** | 인덱스, 쿼리 최적화, Connection Pooling |
| **API** | Edge Functions, 응답 캐싱 |
| **Frontend** | Code Splitting, Image Optimization, SSG/ISR |
| **Matching** | 알고리즘 시간복잡도 O(n²) 이하 유지 |

### 10.2 성능 목표

| 메트릭 | 목표 |
|--------|------|
| 페이지 로드 (LCP) | < 2.5s |
| API 응답 시간 | < 200ms |
| 매칭 실행 시간 | < 3s (50명 기준) |
| Lighthouse Score | > 90 |

---

## 11. Dependency Map

```
┌─────────────────────────────────────────────────────────────┐
│                    Dependencies                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Production:                                                 │
│  ├── next (14.x)                                            │
│  ├── react (18.x)                                           │
│  ├── @supabase/supabase-js                                  │
│  ├── jose (JWT)                                             │
│  ├── bcryptjs (해싱)                                        │
│  ├── zod (검증)                                             │
│  ├── tailwindcss                                            │
│  └── @radix-ui/* (shadcn/ui 기반)                           │
│                                                              │
│  Development:                                                │
│  ├── typescript                                              │
│  ├── eslint                                                  │
│  ├── prettier                                                │
│  └── @types/*                                                │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

**END OF DOCUMENT**

*이 문서는 PRD.md를 기반으로 한 시스템 아키텍처 명세입니다.*
